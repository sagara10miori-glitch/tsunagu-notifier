name: Check

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

# 🔥 GitHub Actions の詰まり防止（必須）
concurrency:
  group: tsunagu-check
  cancel-in-progress: true

# 🔥 push を確実に成功させるための権限
permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 3

    env:
      TZ: Asia/Tokyo
      PIP_DEFAULT_TIMEOUT: "60"

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run notifier
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          PROXY_URL: ${{ secrets.PROXY_URL }}
        run: |
          python notify.py --quiet --retry=3

      # 🔥 last_all.json を送信成功後にのみ更新するためのコミット処理
      - name: Commit updated cache
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          # 競合防止（毎分実行で必須）
          git pull --rebase

          git add data/last_all.json data/seller_cache.json
          git commit -m "Update cache" || echo "No changes"
          git push

      - name: Notify failure
        if: failure()
        run: |
          curl -H "Content-Type: application/json" \
            -d "{\"content\": \"❌ GitHub Actions が失敗しました\"}" \
            ${{ secrets.DISCORD_WEBHOOK_URL }}
